language: python
python:
  - "2.7"
  - "3.6"
before_install:
  - pip install -U pip
install:
  - pip install codecov
  - pip install -r requirements.txt
  - pip install -e .[test]
script:
  - coverage run --source=satmeta -m py.test -v
after_success:
  - codecov
